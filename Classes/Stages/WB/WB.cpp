#include "./WB.h"

//construtor da classe
WB::WB()
{
}

//destrutor da classe
WB::~WB()
{
}

//função que executa o estágio IF
void WB::execute(MEM_WB mem_wb)
{
  /*
  int MemToReg = ex_mem.controlSignal.MemToReg;
  if(MemToReg == 0)
  {
    write register é o rd //isso é o wrIndex
    write data é o aluResult
    O registrador rd vai receber o aluResult //Aquele register la do ID
  }
  else if(MemToReg == 1)
  {
    write register é o rt //isso é o wrIndex
    write data é o readData
    wrIndex = readData; 
    O registrador rt vai receber o readData //Aquele register la do ID
  }
  */
}